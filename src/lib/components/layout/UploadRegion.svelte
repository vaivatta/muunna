<script lang="ts">
	import { duration, fade } from "$lib/util/animation";
	import { dropping, effects } from "$lib/store/index.svelte";
	import { quintOut } from "svelte/easing";
</script>

{#if $dropping}
	<div
		class="fixed w-screen h-screen opacity-40 dynadark:opacity-20 z-[100] pointer-events-none blur-2xl bg-accent-blue"
		transition:fade={{
			duration,
			easing: quintOut,
		}}
	></div>
{/if}
